import "server-only";

// Temporary switch for requiring an order number before accessing audio files.
// Keep this `false` for public access, then set it to `true` to restore login.
export const ORDER_LOGIN_ENABLED = false;
